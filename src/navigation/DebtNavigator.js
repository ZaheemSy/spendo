import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';

const Stack = createStackNavigator();

const DebtNavigator = () => {
  return (
    <Stack.Navigator>
      {/* Debt management screens will be added here */}
    </Stack.Navigator>
  );
};

export default DebtNavigator;

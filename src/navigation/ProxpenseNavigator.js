import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';

const Stack = createStackNavigator();

const ProxpenseNavigator = () => {
  return (
    <Stack.Navigator>
      {/* Proxpense screens will be added here */}
    </Stack.Navigator>
  );
};

export default ProxpenseNavigator;
